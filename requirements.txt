# Project Sentinel — Python deps
requests>=2.31.0        # đủ để gọi LLM thật qua REST (OpenRouter / DeepSeek, OpenAI-compatible)
fastapi>=0.115.0        # Live demo UI (Tuần 1-3) — api/index.py, deploy trên Vercel
jinja2>=3.1.0           # Template HTML cho FastAPI
python-multipart>=0.0.9 # Parse form POST (nút "Chạy Agent" / "Chạy bộ test")
uvicorn[standard]>=0.30.0  # Chạy local: uvicorn api.index:app --reload (Vercel tự cấp ASGI server)

# Tuỳ chọn — SDK openai (nếu cài được); không có vẫn chạy real qua requests
# openai>=1.40.0

# Tuỳ chọn — vector DB (nếu không có sẽ dùng BOW/TF-IDF fallback trong rag/)
# chromadb>=0.5.0
