# Secrets & env
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Data lake runtime
data-lake/*.db
data-lake/*.sqlite
data-lake/reports/*.json
!data-lake/reports/.gitkeep
# Demo snapshot committed on purpose — mentor cần xem dữ liệu thật khi mở repo/live demo Vercel
!data-lake/vuln_data.db
!data-lake/reports/semgrep-report.json

# Scan artifacts (local)
semgrep-report.json
zap-report.json
zap.yaml
*.log
!data-lake/reports/semgrep-report.json

# Juice Shop (local installs / build)
juice-shop/node_modules/
juice-shop/frontend/node_modules/
juice-shop/logs/
juice-shop/data/*.sqlite
juice-shop/data/*.db
juice-shop/.nyc_output/
juice-shop/coverage/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Claude Code local tooling — cài lại bằng lệnh trong CLAUDE.md, không commit
.claude/

# Tài liệu đề bài gốc (PDF cá nhân, không phải sản phẩm của project)
/\[NCUD-GPAI\]*.pdf

# RAG local store (regenerated by rag/ingest.py) — commit snapshot on purpose:
# Vercel không tự chạy rag/ingest.py khi build, thiếu file này làm cả app crash
# (rag/query.py raise SystemExit khi thiếu index, không phải Exception thường).
rag/store/*
!rag/store/bow_index.pkl
!rag/store/knowledge_graph.json
!rag/store/ingest_meta.json

