# CLAUDE.md

Xem [`AGENTS.md`](AGENTS.md) — quy tắc và bản đồ thư mục dùng chung cho mọi AI coding agent
(Claude Code, Cursor, Codex...), không lặp lại riêng cho từng công cụ.

Ghi chú riêng cho Claude Code:

- Skill thiết kế UI/UX cục bộ đã cài tại `.claude/skills/ui-ux-pro-max/` (không commit, chỉ máy dev
  này có — cài lại bằng `npm install -g ui-ux-pro-max-cli && uipro init --ai claude` nếu cần).
- `design-system/project-sentinel/MASTER.md` là design token đã chốt cho live demo — đọc file này
  trước khi đổi màu/font/spacing thay vì đoán lại từ đầu.
